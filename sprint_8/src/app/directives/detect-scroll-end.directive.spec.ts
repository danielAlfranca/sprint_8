import { DetectScrollEndDirective } from './detect-scroll-end.directive';

describe('DetectScrollEndDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectScrollEndDirective();
    expect(directive).toBeTruthy();
  });
});
